"""표 해석 규칙 프롬프트"""

TABLE_READING_RULES = """
# 표 해석 규칙 (입력에 표·수치 비교 데이터가 있을 때만 적용한다)

> 적용 조건: 입력(직접 텍스트, 붙여넣은 표, 엑셀/CSV, 또는 OCR로 추출된 표)에
> 두 값을 비교해 그 차이를 함께 적은 표가 포함된 경우에 적용한다.
> 이 조직의 표에서 '○○비'(추정비·계획비·예산비·목표비 등 '비'로 끝나는 열)는
> 바로 옆 두 항목의 차이(계산값)를 뜻한다. 즉 '기준값 / 실적값 / ○○비' 형태이며,
> ○○비 열에 +/△ 부호가 붙는다. 헤더 단어가 무엇이든(추정비든 예산비든) 이 구조면 적용한다.
> 표가 없으면 이 규칙은 무시한다.
> 표를 해석한 결과는 OUTLINE_GENERATION_RULES의 Fact로 취급되어,
> 관점 사다리·Fact 승격·governing 작성 등 기존 규칙을 그대로 거친다.

## T-1. 부호는 표를 따른다 (계산은 검산용)
- 표에 이미 찍힌 +/△ 부호가 최종 기준이다. 다시 계산해서 부호를 뒤집지 않는다.
- +는 증가·상승·개선(적자라면 적자 폭 축소)으로, △는 감소·하락·악화(적자라면 적자 폭 확대)로 읽는다.
- ★ 무표기 차이값 = + (이해용). 차이값(○○비) 칸에 부호 없이 숫자만 있으면 그 항목은 증가(+) 방향으로 이해한다. 단위 불문(금액·건수·%p 모두). 단, 이는 의미를 이해할 때만 참고하는 것이고, 출력 표기는 원본을 그대로 따른다 — 원본에 부호가 없으면 초안에도 부호를 새로 붙이지 않고, △가 있으면 △ 그대로 옮긴다(표식 있으면 있게, 없으면 없게).
- ※ 측정값 칸(추정 5.0%, 실적 3.5%, 매출 767억처럼 증감이 아닌 절대값)은 부호 개념이 없다. +를 붙이지 말고 값 그대로 읽고 옮긴다. '무표기=+'는 증감을 나타내는 차이값 칸에만 해당한다.
- 검산은 '차이의 크기(절댓값)'만 확인한다. ○○비 열의 값이 옆 두 값의 차이 절댓값과 같은지만 본다(예: 기준 767 / 실적 701 → 차이 66, 표기 △66의 숫자 66과 일치). 뺄셈 방향(앞−뒤인지 뒤−앞인지)이나 부호 방향은 계산으로 판정하지 않는다 — 방향·부호는 표마다 다를 수 있으므로 표를 그대로 신뢰한다.
- 즉 검산은 부호를 바꾸기 위한 것이 아니라 숫자 오독(자릿수 등)을 잡기 위한 것이다. 워딩(+/△)은 항상 표의 부호를 따른다.

## T-2. 불일치 시 사용자에게 확인하고 진행한다 (특히 이미지·OCR 입력)
- 검산(차이 절댓값)이 표의 ○○비 숫자와 다르면, 임의로 추측해서 초안을 진행하지 말고 어느 항목이 어떻게 어긋나는지 짚어 사용자에게 확인을 요청한 뒤 답을 받아 진행한다(예: 기준·실적·차이 셋이 산술적으로 안 맞는 경우 — 자릿수 오독 가능성).
- 입력이 이미지·OCR 경유면 "표가 틀렸다"보다 "내가 잘못 읽었다(OCR 오독)"를 먼저 의심한다. 숫자나 부호가 흐릿해 헷갈리면 그 항목을 콕 집어 "○○를 △XX로 읽었는데 맞습니까?"처럼 되묻는다.
- 모든 항목이 검산과 일치하면 조용히 통과한다(멀쩡한 표에 불필요한 확인 요청을 남발하지 않는다). 뺄셈 방향이나 부호 방향이 직관과 달라 보여도(예: 적자 항목, 비용 항목) 그것만으로 "이상하다"며 확인을 요청하지 않는다 — 절댓값이 맞으면 표를 신뢰하고 그대로 진행한다.
- 차이값 칸에 부호가 없는 것은 정상(= + 증가)이다. 부호 누락을 OCR 오독으로 의심해 확인을 요청하지 않는다(T-1 참조).

## T-3. OCR 산출물 정규화·신뢰
- OCR이 뱉은 부호 유사문자(Δ, ▲, -, 마이너스 계열)는 표준 △로, +계열은 표준 +로 통일한 뒤 읽는다.
- OCR이 비운 칸이나 깨진 보조수치(괄호 안 세부수치, 위첨자 등)는 그럴듯한 값으로 지어내지 않는다. 누락된 칸은 비워 두고 지어내지 않는다.
- 표가 중요한 보고(수치가 핵심 근거)일 때는 초안을 바로 내지 말고, 읽어들인 표 값을 한 번 정리해 보여주고 "이대로 맞으면 진행하겠습니다"로 확인받은 뒤 작성한다.

## T-4. 표 위계를 보고 위계로 매핑
- 표의 상위·합계 항목(예: Total → 사업부 → 지역/법인)을 항목 라벨로 올리고, 그 구성·변동 요인을 하위에 둔다.
  · 수치 라벨 형식: [항목] [차이부호][차이값] ([기준값] → [실적값])  (예: SK매직 국내 △20억 (64억 → 44억))
- 그 상위를 구성·설명하는 하위 항목(구성 분해, 변동 요인 등)은 그 아래 · 세부 항목으로 내린다. 각 ·은 반드시 새 줄에서 시작한다(한 줄에 · 2개 이상 금지 — S-1 규칙과 동일).
- ★ 충돌 해소 (중요) — 위 '수치 라벨'은 '실적·추정비 분석' 성격의 보고에서 표를 풀어 제시하는 별도 형식이다. 따라서:
  (1) 이 수치 라벨은 4장·0-2의 '메시지형 heading 규칙'(활동·수치로 끝나는 heading 금지, Outcome 판단문 강제)의 적용 대상이 아니다. 실적 분석 영역에서는 'SK매직 국내 △20억' 같은 수치 라벨을 그대로 항목 머리로 쓸 수 있다(이미지의 실제 보고 형식).
  (2) 단, 슬라이드 전체를 관통하는 governing·Key Message(장표 제목·전체 결론)는 여전히 4장 규칙대로 30자 내외 Outcome/Impact 판단문으로 쓴다. 수치 라벨을 슬라이드 전체 governing 자리에 그대로 올려 'governing을 수치 나열로 대체'하지 않는다.
  (3) 요약: 수치 라벨 = 실적 분석 항목의 머리(허용), governing = 장표 전체 결론(판단문 유지). 이 둘은 다른 층위다.

## T-5. 중요도 선별 (짜잘한 항목까지 다 읽지 않는다)
표의 모든 셀을 기계적으로 문장화하지 않는다. 아래 기준으로 추린다.
- 상위 항목(실적 분석 항목 라벨 또는 헤드라인)으로 올릴 것: 합계·상위 항목, 그리고 차이값이 크거나 / 흑↔적 등 방향 전환이 있거나 / 기준 대비 크게 벗어난 항목.
- 하위 ·로 내릴 것: 그 상위의 변동을 주도한 핵심 구성요소 몇 개. 전부 나열이 아니라 변동 주도분 위주.
- 생략할 것: 차이가 미미하거나(반올림 수준) 상위 메시지에 영향이 없는 항목(예: 거의 안 움직인 누적계정).
- 깊이는 모드가 정한다: 서머리는 상위 항목 위주로 얕게, 심층은 하위까지 펼치되 '변동 주도 요인' 기준으로 추린다. 즉 모드로 깊이를 정하고 그 안에서 중요도로 거른다.

## T-6. 원인·요인은 추정 금지
- 표에서 읽을 수 있는 것은 수치·부호·증감 방향(+/△가 뜻하는 증가·감소)까지다. "신규계정 감소 때문" 같은 변동 원인은 표만으로 단정하지 않는다.
- 원인이 입력에 함께 제공돼 있으면 그대로 쓰고, 없으면 지어내지 말고 원인 서술을 생략한다.

## T-7. 초안에 표를 인용할 때
- 표의 일부를 초안 본문에 그대로 넣을 경우, 원본 부호 표기와 단위(억원/%/%p/원/대 등)를 그대로 유지한다. 원본에 △가 있으면 △로, +가 있으면 +로, 부호가 없으면 부호 없이(임의로 +를 붙이지 않는다) 옮긴다.
- 12-1 표 우선 적용 대상(비교·현황 요약 등)에 해당하면 Bullet이 아니라 표로 배치한다(기존 12장 규칙 적용).
"""


#── 보고 목적별 구조 ───────────────────────────────────────────
